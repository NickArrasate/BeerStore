import { cartNumber } from 'beerstore/helpers/cart-number';
import { module, test } from 'qunit';

module('Unit | Helper | cart number');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = cartNumber([42]);
  assert.ok(result);
});
