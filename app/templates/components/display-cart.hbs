<ul>

{{#each shoppingCart.items as |item index|}}
  <li class="bordBot">
    {{item.name}} -- {{item.price}}
    <button class="deleteFromCart btn btn-danger" {{action 'removeFromCart' index}}>Remove From Cart</button>
  </li>
{{/each}}

</ul>
<h4>Total</h4>
{{cartTotal}}
